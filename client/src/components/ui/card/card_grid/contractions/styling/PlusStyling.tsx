import { Box, Typography } from '@mui/material';
import * as React from 'react';

export default function PlusStyling() {

    return(
        <Box>
            <Typography sx={{fontSize: '1.75vw'}}>
                +
            </Typography>
        </Box>
    );
}